# TinyFingers

A fast fullscreen keyboard, pointer, and touch toy for toddlers, built with Astro and the Canvas API.

```sh
npm install
npm run dev
```

Production checks:

```sh
npm test
npm run build
```
